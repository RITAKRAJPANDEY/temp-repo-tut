// Modules
//Common JS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum )


const SayHi=(name)=>{
    console.log(`Hello There ${name}`)
}
module.exports = SayHi//to export a Function